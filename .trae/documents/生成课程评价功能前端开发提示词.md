我将为您生成一份详细的前端开发提示词，用于指导前端开发者对接课程评价功能。

### 计划内容

1.  **接口梳理**：
    *   **发布评论接口** (`POST /api/v1/course/comment`)：明确请求参数（`course_id`, `rating`, `tags`, `content`）及其校验规则（如内容限300字）。
    *   **获取评论列表接口** (`GET /api/v1/course/comment/{course_id}`)：明确分页参数（`page`, `size`）和响应数据结构（包含用户信息、评分、标签等）。

2.  **编写前端提示词**：
    *   **功能描述**：说明这是课程详情页下的评价模块。
    *   **交互逻辑**：
        *   **评价表单**：星级评分组件、标签选择（可自定义或预设）、文本输入框（带字数统计）。
        *   **评价列表**：无限滚动或分页展示，展示用户头像、昵称、星级、标签和内容。
        *   **状态管理**：提交评价后的列表刷新逻辑。
    *   **数据结构参考**：提供具体的 JSON 示例。

### 预期输出
一段清晰、可直接使用的 Prompt，包含接口文档摘要和前端组件开发建议。